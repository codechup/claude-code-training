---
title: 'Subagents and custom agents'
description: 'Delegate work to subagents with their own tools, model, and permissions.'
order: 10
---

This module delegates work to subagents — separate agents with their own tools, model, and permissions, launched from inside a session. You will meet the built-in agents, learn the frontmatter that defines a custom one, build a small set of them for real tasks, control which model each one runs on, and use fork, background, and worktree isolation for agents that should not share your main session's state.

1. `01-agent-tool-and-builtins` — The Agent tool; Explore, Plan, general-purpose, claude, statusline-setup, claude-code-guide (15 min, core).
2. `02-custom-agents` — `.claude/agents/*.md` frontmatter (`tools`, `disallowedTools`, `model`, `permissionMode`, `maxTurns`, `skills`, `mcpServers`, `hooks`, `memory`, `effort`, `isolation`, `color`) (20 min, core).
3. `03-agent-lab` — Lab: code-reviewer (read-only, Sonnet), test-writer, docs-writer, researcher (30 min, core).
4. `04-model-per-agent` — Model resolution order, `CLAUDE_CODE_SUBAGENT_MODEL`, cost strategy (15 min, core).
5. `05-fork-background-worktree` — Fork agents, background agents, worktree isolation, Monitor (20 min, advanced).
6. `06-advisor` — The advisor pattern: when a second, stronger reviewer pays off (10 min, advanced).
