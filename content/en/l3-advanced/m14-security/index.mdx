---
title: 'Working securely'
description: 'Sandboxing, prompt injection, secrets, and data retention.'
order: 14
---

This module covers what keeps agentic work safe: the permission model and sandbox that contain what a session can touch, how prompt injection reaches an agent through files and tool results and how to defend against it, handling secrets so they never land in a public repository, and the managed-settings and data-retention controls an organisation relies on.

1. `01-permission-model-and-sandbox` — Sandboxing (filesystem, network allowlist), auto-mode containment, what `bypassPermissions` really does (20 min, core).
2. `02-prompt-injection` — Injection through files, web pages and tool results; defences (20 min, core).
3. `03-secrets` — `.env` handling, gitleaks, a hook that blocks secret leaks, public-repo hygiene (20 min, core).
4. `04-managed-settings` — Managed and server-managed settings, `auto-mode-config`, allowlists (15 min, advanced).
5. `05-data-and-retention` — Data usage, zero-data-retention, telemetry, monitoring usage (10 min, advanced).
