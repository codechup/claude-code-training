# Transcript — m04-commands/05-subcommands

Raw Claude Code session recordings for this lesson's lab. **Real recordings only** (D070, D093,
D099). This lesson has no lab-repo tag (`repo_tag: 'none'`): every command inspects the machine
Claude Code is installed on, not a checked-out repository, so the recordings were made on the
writing session's own machine.

Trimming means deleting lines and redacting local paths, usernames and anything
`.claude/rules/public-hygiene.md` forbids — never rewriting output into something cleaner than
the tool produced. The three `--help` captures are filtered to the first line of each subcommand's
description (`grep -E '^  [a-z]'`, recorded in each file's provenance header); the terminal wraps
those descriptions over several lines and the continuation lines add nothing. No text was rewritten.

Files here are outside the content collections and outside the EN/TR parity gate.

| File                        | Lab step | Recorded on | Claude Code version |
| --------------------------- | -------- | ----------- | ------------------- |
| `01-claude-mcp.txt`         | 4        | 2026-09-07  | 2.1.263             |
| `02-claude-plugin.txt`      | 5        | 2026-09-07  | 2.1.263             |
| `03-claude-subcommands.txt` | 1        | 2026-09-07  | 2.1.263             |
| `04-env-var-model.txt`      | 7        | 2026-09-07  | 2.1.263             |
