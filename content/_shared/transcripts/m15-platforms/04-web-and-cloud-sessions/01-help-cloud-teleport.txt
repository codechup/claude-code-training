# Captured 2026-09-07 against codechup/claude-code-lab at lesson/m15-01-start, Claude Code 2.1.263 (native install, Windows 11). Local paths and username redacted; nothing else changed. Trimmed: only the grep-matched flag entries are kept.
› claude --help | grep -A2 -- '--cloud \['
  --cloud [description|session_id|url]  Create a cloud session with the given
                                        description, or attach to an existing
                                        one by session ID or claude.ai/code URL
› claude --help | grep -A1 -- '--teleport \['
  --teleport [session]                  Resume a teleport session, optionally
                                        specify session ID
