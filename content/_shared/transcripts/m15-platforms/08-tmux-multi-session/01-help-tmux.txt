# Captured 2026-09-07 against codechup/claude-code-lab at lesson/m15-01-start, Claude Code 2.1.263 (native install, Windows 11). Local paths and username redacted; nothing else changed.
› claude --help | grep -A3 -- '--tmux'
  --tmux                                Create a tmux session for the worktree
                                        (requires --worktree). Uses iTerm2
                                        native panes when available; use
                                        --tmux=classic for traditional tmux.
› claude --help | grep -A2 -- '--worktree'
  -w, --worktree [name]                 Create a new git worktree for this
                                        session (optionally specify a name)
