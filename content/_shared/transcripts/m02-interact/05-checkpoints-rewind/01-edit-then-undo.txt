# Captured 2026-09-07 against codechup/claude-code-lab @ lesson/m02-05-start, Claude Code 2.1.263. Verbatim. The --settings flag on the command line overrides this machine's Turkish language preference.
› claude -p "isOverdue() in src/store.ts compares milliseconds against seconds, so overdue tasks are never reported. Fix it with a single Edit and run npm test." --model sonnet --output-format text --settings '{"language":"English"}' --max-turns 16 --permission-mode acceptEdits --allowedTools "Read,Edit,Glob,Grep,Bash(npm test)"
Fixed: `isOverdue()` now compares `Date.parse(task.dueDate)` directly against `now()`, both in milliseconds. All 15 tests pass.
