# Captured 2026-09-07, Claude Code 2.1.263, Windows 11 with Git Bash, Node 24.18.0. Absolute scratch-directory paths were replaced with <scratch>, the home directory with ~, and terminal hyperlink escape codes were stripped; nothing else was changed.
$ claude mcp get reporting
reporting:
  Scope: Project config (shared via .mcp.json)
  Status: ⏸ Pending approval (run `claude` to approve)
  Type: http
  URL: ${REPORTING_MCP_URL:-https://mcp.example.com/mcp}
  Headers:
    Authorization: Bearer ${REPORTING_MCP_TOKEN}

To remove this server, run: claude mcp remove reporting -s project

$ claude mcp list
Checking MCP server health…

reporting: ${REPORTING_MCP_URL:-https://mcp.example.com/mcp} (HTTP) - ⏸ Pending approval (run `claude` to approve)

MCP config diagnostics ⚠

For help configuring MCP servers, see: https://code.claude.com/docs/en/mcp

[Contains warnings] Project config (shared via .mcp.json)
Location: <scratch>\lab07\.mcp.json
 └ [Warning] [reporting] mcpServers.reporting: Missing environment variables: REPORTING_MCP_TOKEN
