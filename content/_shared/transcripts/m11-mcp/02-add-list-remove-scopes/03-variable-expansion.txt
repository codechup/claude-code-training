# Captured 2026-09-07, Claude Code 2.1.263, Windows 11 with Git Bash, Node 24.18.0. Absolute scratch-directory paths were replaced with <scratch>, the home directory with ~, and terminal hyperlink escape codes were stripped; nothing else was changed.
$ cat .mcp.json
{
  "mcpServers": {
    "claude-code-docs": {
      "type": "http",
      "url": "${DOCS_MCP_URL:-https://code.claude.com/docs/mcp}",
      "headers": {
        "Authorization": "Bearer ${DOCS_MCP_TOKEN}"
      }
    }
  }
}

$ claude mcp list
Checking MCP server health…

claude-code-docs: ${DOCS_MCP_URL:-https://code.claude.com/docs/mcp} (HTTP) - ⏸ Pending approval (run `claude` to approve)
cc-tools: claude mcp serve - ✔ Connected

MCP config diagnostics ⚠

For help configuring MCP servers, see: https://code.claude.com/docs/en/mcp

[Contains warnings] Project config (shared via .mcp.json)
Location: <scratch>\lab02-start\.mcp.json
 └ [Warning] [claude-code-docs] mcpServers.claude-code-docs: Missing environment variables: DOCS_MCP_TOKEN
