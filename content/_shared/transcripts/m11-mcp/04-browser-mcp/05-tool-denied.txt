# Captured 2026-09-07, Claude Code 2.1.263, Windows 11 with Git Bash, Node 24.18.0. Absolute scratch-directory paths were replaced with <scratch>, the home directory with ~, and terminal hyperlink escape codes were stripped; nothing else was changed.
# Command: claude -p "Use the playwright MCP server: open http://localhost:4429/ and report the page title. If you cannot, say exactly which tool was unavailable." --model sonnet --max-turns 5 --output-format text --settings en.json --mcp-config mcp-pw.json --strict-mcp-config --allowedTools "ToolSearch"
# en.json is {"language": "English"} — this machine has "language": "Turkish" in its user settings; nothing else was overridden.
The `mcp__playwright__browser_navigate` tool call was blocked because permission wasn't granted for it.

**Tool unavailable: `mcp__playwright__browser_navigate`** (permission not granted). I cannot open the page or retrieve its title without this being approved.
