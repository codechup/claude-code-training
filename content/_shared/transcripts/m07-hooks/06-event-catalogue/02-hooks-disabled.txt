# Captured 2026-09-07, Claude Code 2.1.263, the same scratch project and thirteen-event settings file as 01-which-events-fire.txt, with events.log deleted and disableAllHooks passed on the command line for this run only. CLAUDE_CODE_DISABLE_AUTO_MEMORY=1; the same --settings flag also carried "language":"English". Verbatim.
› rm -f events.log
› claude -p "Read notes.txt and reply with one sentence." --model sonnet --max-turns 3 --output-format text --allowedTools "Read" --settings '{"disableAllHooks":true}'
The notes state that labtrack ships a CLI and an HTTP API, and its overdue check compares dates in seconds.

› ls events.log
ls: cannot access 'events.log': No such file or directory
