# Captured 2026-09-07, lab repo at tag lesson/m01-04-start after the headless session above. Command: git diff. Verbatim.
› git diff
diff --git a/src/store.ts b/src/store.ts
index 935e88a..a2bfa9d 100644
--- a/src/store.ts
+++ b/src/store.ts
@@ -21,8 +21,10 @@ export interface AddTaskInput {
 }
 
 export function addTask(input: AddTaskInput): Task {
-  const title = input.title;
+  const title = input.title.trim();
+  if (!title) throw new InvalidTaskError("title must not be empty");
   const priority = input.priority ?? 0;
+  if (priority < 0) throw new InvalidTaskError("priority must not be negative");
   const task: Task = {
     id: randomUUID(),
     title,
