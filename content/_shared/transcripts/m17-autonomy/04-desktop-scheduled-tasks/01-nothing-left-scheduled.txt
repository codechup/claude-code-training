# Captured 2026-09-07, Claude Code 2.1.263, from PowerShell after every capture in this module: Test-Path "$env:USERPROFILE\.claude\scheduled-tasks"; Get-ChildItem "$env:USERPROFILE\.claude" -Filter "*cron*" -Recurse — verbatim stdout. This is the module's clean-up check: no desktop scheduled-task folder was ever created, and no cron task was written to disk by the /loop captures, because session-scoped tasks die with the process.
False
--- scheduled-tasks dir exists? above ---
--- cron files above (none printed = none) ---
