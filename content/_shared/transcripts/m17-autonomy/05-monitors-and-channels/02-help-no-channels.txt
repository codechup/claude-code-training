# Captured 2026-09-07, Claude Code 2.1.263, from PowerShell: $h = claude --help | Out-String; ($h -split "`n" | Select-String -Pattern "channel").Count — verbatim stdout. Zero lines of `claude --help` mention "channel", which is what the channels doc says to expect while the feature is in research preview.
0
channel-matches-above
