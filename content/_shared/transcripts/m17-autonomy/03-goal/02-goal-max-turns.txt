# Captured 2026-09-07, Claude Code 2.1.263, in the lab repo on tag lesson/m17-01-solution, run from PowerShell: claude -p "/goal scripts/loop-check.sh has been run once with bash and its exit code stated in this conversation, or stop after 3 turns" --model sonnet --max-turns 6 --allowedTools "Bash(bash scripts/loop-check.sh)" --output-format text — verbatim stderr/stdout; a workspace-trust warning naming a local path was deleted from the top. With that narrow Bash allowlist pattern the run never reached a state the evaluator accepted, so the goal loop kept starting turns until --max-turns ended the process. Re-run on 2026-09-08 with a language override, the same command resolved instead (num_turns 2) — the outcome is not deterministic, which is why the lesson teaches the ceiling rather than the failure.
Error: Reached max turns (6)
exit=1
