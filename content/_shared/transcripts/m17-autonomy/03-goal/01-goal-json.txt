# Captured 2026-09-08, Claude Code 2.1.263, in the lab repo on tag lesson/m17-01-solution: claude -p "/goal scripts/loop-check.sh has been run once with bash and its exit code stated in this conversation, or stop after 3 turns" --settings <a one-key file setting "language": "English"> --model sonnet --max-turns 8 --allowedTools Bash --output-format json | jq '{subtype, is_error, num_turns, total_cost_usd, modelUsage: (.modelUsage|keys), result}' — verbatim jq output, every other envelope field deleted. The --settings override is only there because the capturing machine's user settings ask for Turkish.
{
  "subtype": "success",
  "is_error": false,
  "num_turns": 2,
  "total_cost_usd": 0.04752095,
  "modelUsage": [
    "claude-haiku-4-5-20251001",
    "claude-sonnet-5"
  ],
  "result": "The goal is met: `scripts/loop-check.sh` was run once via bash, and its exit code was **0** (all tests passed, \"green — nothing to do\")."
}
exit=0
