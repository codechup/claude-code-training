# Captured 2026-09-07, Claude Code 2.1.263, in a scratch folder holding only CLAUDE.md and notes/conventions.md. CLAUDE_CODE_DISABLE_AUTO_MEMORY=1. Verbatim.
› claude -p "Without reading any files, answer in one sentence: how is this project released, and what must a pull request title start with?" --model sonnet --max-turns 3 --output-format text --disallowedTools "Read,Glob,Grep,Bash,WebFetch,WebSearch"
Proje `make ship` ile yayınlanıyor (asla `npm publish` değil) ve pull request başlıkları köşeli parantez içinde bir ticket ID ile başlamalı, örneğin `[LAB-42] fix pagination`.
