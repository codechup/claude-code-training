# 2026-09-07 · claude -p "Fix the bug in math.js so add() returns a + b, then tell me in one sentence what you did." --permission-mode acceptEdits  --model sonnet --max-turns 4 --output-format text · Claude Code 2.1.263
$ claude -p "Fix the bug in math.js ..." --permission-mode acceptEdits  --model sonnet --max-turns 4 --output-format text
`add()` fonksiyonundaki `a - b` ifadesini `a + b` olarak değiştirdim, böylece fonksiyon artık doğru şekilde toplama işlemi yapıyor.

$ cat math.js
export function add(a, b) {
  return a + b;
}
