# 2026-09-07 · claude -p "Fix the bug in math.js so add() returns a + b, then tell me in one sentence what you did." --permission-mode default  --model sonnet --max-turns 4 --output-format text · Claude Code 2.1.263
$ claude -p "Fix the bug in math.js ..." --permission-mode default  --model sonnet --max-turns 4 --output-format text
Dosyayı düzenlemek için izin bekliyorum; lütfen onaylayın ki `add()` fonksiyonunu `a + b` döndürecek şekilde düzeltebileyim.

$ cat math.js
export function add(a, b) {
  return a - b; // BUG: subtracts
}
