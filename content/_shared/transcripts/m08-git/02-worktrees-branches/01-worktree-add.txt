# Captured 2026-09-07, Claude Code 2.1.263, throwaway clone of codechup/claude-code-lab at tag lesson/m08-02-start.
# Shell commands are shown with a $ prompt; the claude -p run between them is verbatim stdout.
# Absolute local paths redacted to ~/claude-code-lab.
$ git worktree list
~/claude-code-lab 202fdfa [lab-m08-02]

$ claude -p "Create a git worktree at ../claude-code-lab-overdue on a new branch named fix-overdue, then run 'git worktree list'. Reply with only the raw output of that final git worktree list command, in a fenced code block, and no other text." --allowedTools "Bash(git *)" --model sonnet --max-turns 20 --output-format text
```
~/claude-code-lab         202fdfa [lab-m08-02]
~/claude-code-lab-overdue 202fdfa [fix-overdue]
```

$ git worktree list
~/claude-code-lab         202fdfa [lab-m08-02]
~/claude-code-lab-overdue 202fdfa [fix-overdue]

$ git -C ../claude-code-lab-overdue branch --show-current
fix-overdue
