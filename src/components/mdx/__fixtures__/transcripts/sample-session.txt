Formatting sample only — not a recorded Claude Code session. Real lessons render a transcript captured from the lab repo (D070/D093).
› why does the test in auth.test.ts fail?
⏺ Read src/auth.test.ts
⏺ Bash(npx vitest run auth)  exit 1
⏺ Edit src/auth.ts  +2 −1
✓ Fixed: token expiry compared seconds to ms.
