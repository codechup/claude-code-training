---
// Fixture: only token references — no raw color literals.
---

<div class="bg-bg text-fg">
  Uses tokens, not raw colors. Also safe: an HTML entity that happens to look hex-ish, &#9788;, must
  not be flagged.
</div>
