## Plan

- Plan: `PNN` — <title> (see `plans/PNN-*.md`)
- Milestone: M0 | M1 | M2 | M3 | M4

## What changed

-

## How to try it

```bash
# commands a reviewer can run locally
```

## Evidence

- [ ] Screenshot(s) attached (UI changes; phone 390px + desktop 1280px when relevant)
- [ ] Live URL (release plans only)

## Definition of Done

- [ ] CI green (typecheck / lint / content gate / vitest / build / e2e-a11y / links / lighthouse as applicable)
- [ ] Tests for this plan's scope added and passing
- [ ] Docs updated where behaviour changed (`docs/`, `DECISIONS.md`, `CLAUDE.md`)
- [ ] Plan frontmatter set to `review` (→ `done` after merge) and `STATE.md` regenerated (`npm run plan state`)
- [ ] No files touched outside the plan's `owned_paths` (or listed in `shared_paths` with an append-only change)

## Handoff notes

<!-- anything the next session must know: follow-ups, known gaps, decisions taken -->
