# Claude Code feature inventory — verified 2026-09-06 (Claude Code 2.1.263)

> Source of truth for lesson writers. Every item was checked against `https://code.claude.com/docs/en/<slug>.md` on the date above. Items marked **UNVERIFIED** must not appear in lesson text until resolved (see `open_questions` of P03). Re-verify with `/verify-sources` before each release.

## Official docs map (`https://code.claude.com/docs/en/<slug>.md`)

- **Getting started:** overview · quickstart · changelog
- **Core concepts:** how-claude-code-works · features-overview · claude-directory · context-window · prompt-caching
- **Use Claude Code:** memory · sessions · common-workflows · prompt-library · best-practices
- **Platforms:** platforms · remote-control · mobile · chrome · computer-use · vs-code · jetbrains · slack · claude-tag
- **Web & Desktop:** web-quickstart · claude-code-on-the-web · routines · ultrareview · desktop-quickstart · desktop · desktop-linux · desktop-wsl · desktop-scheduled-tasks · desktop-ios-simulator
- **CI/CD:** security-guidance · claude-security · code-review · github-actions · github-actions-cloud-providers · github-enterprise-server · gitlab-ci-cd
- **Agents & parallel:** agents · sub-agents · agent-view · agent-teams · cross-session-messaging · workflows · worktrees
- **MCP & skills:** mcp-quickstart · mcp · skills · discover-plugins · plugins · artifacts
- **Automation:** hooks-guide · channels · scheduled-tasks · goal · headless · deep-links · large-codebases
- **Troubleshooting:** troubleshoot-install · troubleshooting · debug-your-config · errors
- **Setup & access:** admin-setup · setup · authentication · managed-settings · server-managed-settings · managed-mcp · auto-mode-config
- **Deployment:** third-party-integrations · feature-availability · amazon-bedrock · claude-platform-on-aws · google-vertex-ai · microsoft-foundry · network-config · corporate-launcher · devcontainer · gateways
- **Usage & data:** monitoring-usage · costs · analytics · plugin-marketplaces · plugin-dependencies · plugin-hints · plugin-relevance · security · data-usage · zero-data-retention
- **Configuration:** settings · settings-reference · settings-example · permissions · permission-modes · sandboxing · sandbox-environments · cloud-environments
- **Model & interface:** model-config · fast-mode · advisor · output-styles · terminal-config · fullscreen · accessibility · voice-dictation · statusline · keybindings
- **Reference:** cli-reference · commands · env-vars · tools-reference · interactive-mode · checkpointing · hooks · plugins-reference · channels-reference · glossary
- **Agent SDK:** agent-sdk/overview · quickstart · migration · agent loop · examples · sessions · input/output · tools · subagents · hooks · custom tools · MCP · skills · permissions · checkpointing · cost tracking · observability · deployment · TypeScript/Python API refs

## Feature inventory

| Area | Current facts | Doc |
|---|---|---|
| Install & auth | Native installers (macOS/Linux/Windows), Homebrew, WinGet, apt/dnf/apk, npm `@anthropic-ai/claude-code`; API key, OAuth subscription token (`claude setup-token`), federation, profiles; keyless sign-in with a Console account | setup, authentication |
| Permission modes | `default` (aliased as `manual` in the CLI, VS Code and Desktop labels, and `permissionMode`/`defaultMode` config — requires Claude Code ≥ 2.1.200), `plan`, `acceptEdits`, `auto` (classifier), `dontAsk`, `bypassPermissions` (`permissions.defaultMode: auto|bypassPermissions` is honoured only from user settings, `--settings`, or managed settings — set in project/local `.claude/settings.json`/`settings.local.json` it is silently ignored and the session starts in a fallback mode; verified against `permission-modes.md` 2026-09-08) | permission-modes |
| Sandbox | filesystem + network isolation with allowlist; auto-mode containment rules | sandboxing |
| Memory | memory files are **concatenated in load order** (managed, user `~/.claude/CLAUDE.md`, project `CLAUDE.md`, local `CLAUDE.local.md`, nested directory files) — later files add to, not override, earlier ones (corrected by P15 against the live `memory.md`); `.claude/rules/` path-scoped; auto-memory with MEMORY.md index (first 200 lines / 25 KB), `autoMemoryEnabled`; `@path` imports; `claudeMdExcludes` | memory (re-fetched 2026-09-07) |
| Built-in slash commands | ~50+, incl. `/model /effort /advisor /memory /context /compact /clear /resume /branch /fork /cd /add-dir /teleport /remote-control /plan /background /batch /tasks /subtask /goal /loop /code-review /security-review /diff /simplify /copy /export /help /status /usage /cost /debug /doctor /permissions /mcp /config /init /hooks /keybindings /color /theme /focus /fullscreen /login /logout /exit /rewind /feedback /design /design-sync /deep-research /dataviz /insights /import /install-github-app /install-slack-app /ide /plugin /chrome /fast /btw /mobile /desktop /autocompact /auto-mode-setup /fewer-permission-prompts /claude-api /list-agents /skill-doctor /reload-plugins /web-setup /autofix-pr /ios /android`; `/ios` and `/android` are aliases of `/mobile` (QR code for the mobile app); `/web-setup` (terminal-only, needs a claude.ai sign-in and `gh` CLI) syncs the local GitHub CLI token to claude.ai and creates a **Default** cloud environment for `--cloud`/web sessions; `/autofix-pr [prompt]` spawns a web session that watches the current branch's PR and pushes fixes when CI fails or a reviewer comments (verified `commands.md`, `web-quickstart.md` 2026-09-08) | commands |
| Skills | locations: `~/.claude/skills/<name>/SKILL.md` (personal), `.claude/skills/` (project), `<plugin>/skills/`, nested `subdir/.claude/skills/`, enterprise via managed settings; frontmatter `name, description, when_to_use, argument-hint, arguments, disable-model-invocation, user-invocable, allowed-tools (Bash(pattern) syntax), disallowed-tools, model, effort, context: fork, agent, background, hooks, paths, shell, metadata, license, compatibility`; substitutions `$ARGUMENTS`, `$0`/`$1`/`$ARGUMENTS[N]`, named `$component`, `${CLAUDE_SESSION_ID}`, `${CLAUDE_EFFORT}`, `${CLAUDE_SKILL_DIR}`, `${CLAUDE_PROJECT_DIR}`, `${CLAUDE_PLUGIN_ROOT}`, `${CLAUDE_PLUGIN_DATA}`; backslash escapes a literal `$` | skills (re-fetched 2026-09-06) |
| Hooks | events: SessionStart, Setup, UserPromptSubmit, UserPromptExpansion, PreToolUse, PermissionRequest, PermissionDenied, PostToolUse, PostToolUseFailure, PostToolBatch, Notification, MessageDisplay, SubagentStart, SubagentStop, TaskCreated, TaskCompleted, Stop, StopFailure, TeammateIdle, InstructionsLoaded, ConfigChange, CwdChanged, DirectoryAdded, FileChanged, WorktreeCreate, WorktreeRemove, PreCompact, PostCompact, PreModelSwitch, PostModelSwitch, Elicitation, ElicitationResult, SessionEnd (33 events — SessionEnd was missing here; P19, live `hooks.md` 2026-09-07); stdin/stdout JSON; exit 0 = ok (stdout parsed as JSON when it is an object), 2 = block on blocking events (PreToolUse, UserPromptSubmit, UserPromptExpansion, Stop, SubagentStop, WorktreeCreate, Elicitation, ElicitationResult), other = non-blocking; PreToolUse decisions via `hookSpecificOutput: {hookEventName, permissionDecision: allow|deny|ask|defer (the live page lists these four; `block` is expressed via exit code 2 — P19), permissionDecisionReason, additionalContext?, updatedInput?, systemMessage?}` | hooks (re-fetched 2026-09-06), hooks-guide |
| Subagents | `.claude/agents/*.md` frontmatter `name, description, tools, disallowedTools, model (sonnet|opus|haiku|fable|<id>|inherit), permissionMode, maxTurns, skills, mcpServers, hooks, memory (user/project/local), background, effort, isolation: worktree, color, initialPrompt, experimental`; built-ins Explore, Plan, general-purpose, claude, statusline-setup, claude-code-guide; model order: per-invocation `model` → frontmatter → `CLAUDE_CODE_SUBAGENT_MODEL` (+`_FORCE`) → main model; fork agents inherit context | sub-agents (re-fetched 2026-09-06) |
| MCP | `claude mcp add --transport http <name> <url>` / `--transport stdio -- cmd`; `--scope local|project|user`; list/remove/get/add-json/add-from-claude-desktop/serve/reset-project-choices; `claude mcp login <server>` / `claude mcp logout <server>` (OAuth from the shell, v2.1.186+, `--no-browser` for SSH); scope precedence highest→lowest: local (`~/.claude.json` per-project) → project (`.mcp.json`) → user (`~/.claude.json` global) → plugin-provided → claude.ai connectors, matched by name across scopes or by endpoint URL for plugins/connectors; tool search enabled by default (tools become available on first use without waiting for server connect; disabled by a custom `ANTHROPIC_BASE_URL`, `ENABLE_TOOL_SEARCH=false`, or pre-4.5-generation models on Vertex); `MCP_TIMEOUT` (server startup timeout, ms) and `MAX_MCP_OUTPUT_TOKENS` (default 25,000) env vars, with a 10,000-token warning regardless; `${VAR}` / `${VAR:-default}` in `.mcp.json`; managed MCP config (`managed-mcp.json` on self-hosted hosts, allow/deny lists) for enterprise-wide server deployment; tool `_meta` annotations `anthropic/requiresUserInteraction` (approval on every call, no "don't ask again", denied outright in `dontAsk` mode) and `anthropic/maxResultSizeChars` (per-tool output-size override, up to 500,000 chars); SSE deprecated | mcp |
| Plugins | `plugin.json` (name, description, version, author); dirs `skills/ agents/ hooks/hooks.json .mcp.json .lsp.json monitors/ bin/ settings.json`; `/plugin install|list`; `claude plugin validate`; marketplaces | plugins, plugin-marketplaces |
| Plugin eval | `claude plugin eval` (early access per org) with `--case --tag --runs --model --judge-model --json --threshold --allow-tools --scaffold --ablation --mocks --report`; graders regex/tool_used/tool_order/file_exists/llm/baseline; `/skill-doctor` | plugins-reference |
| Models | Fable 5.1 (1M ctx), Fable 5, Opus 5/4.8/4.7/4.6, Sonnet 5/4.6, Haiku 4.5; aliases `default best fable opus sonnet haiku sonnet[1m] opus[1m] opusplan`; ids `claude-fable-5-1`, `claude-fable-5`, `claude-opus-5`, `claude-sonnet-5`, `claude-haiku-4-5` (Claude Code aliases — real `modelUsage` output reports the dated Claude API ids such as `claude-haiku-4-5-20251001`; P17); the 1M context applies to Fable 5.1 and the `[1m]` aliases, not to every model; Bedrock/Vertex/Foundry carry older sets; `modelPicker` curation | model-config (re-checked 2026-09-07) |
| Effort / fast | `/effort low|medium|high|xhigh|max` (default `high`; Opus 4.7 defaults to `xhigh`), `/effort ultracode` (a Claude Code setting that plans dynamic workflows; needs ≥ 2.1.203), `/effort auto` clears; `max` is session-only unless `CLAUDE_CODE_EFFORT_LEVEL`; persisted per model in `modelSettings.<id>.effortLevel` or global `effortLevel`; `ultrathink` keyword for one turn; Opus 4.6/Sonnet 4.6 lack `xhigh`. **Fast mode is not an effort level**: `/fast` serves Claude Opus with a speed-prioritising API configuration at identical quality (priced $10/$50 per MTok per the live `fast-mode.md`, 2026-09-07, P17); it refuses when it would switch to an Opus outside `availableModels` | model-config, fast-mode (re-checked 2026-09-07) |
| Headless | `claude -p` with `--bare --allowedTools --disallowedTools --permission-mode --output-format json|stream-json|text --json-schema --continue --resume --max-turns --model --append-system-prompt --settings --mcp-config --agents --plugin-dir --plugin-url --permission-prompts none`; `--disallowedTools` (`--disallowed-tools`) is a deny list — a bare tool name removes it from context (e.g. `Edit`, `*`, `mcp__*`), a scoped rule like `Bash(rm *)` leaves the tool available but denies matching calls (verified `cli-reference.md` 2026-09-08); exit codes 0/1/2 (partial)/130/143 | headless |
| Agent SDK | `claude-agent-sdk` (Python), `@anthropic-ai/claude-agent-sdk` (TS): agent loop, hooks, permissions, sessions, skills, MCP, custom tools, subagents, plugins | agent-sdk/overview |
| GitHub Actions | `anthropics/claude-code-action@v1` (`prompt`, `claude_args`, `anthropic_api_key` or `claude_code_oauth_token`, `plugin_marketplaces`, `plugins`, `settings`); interactive (@claude) or automation mode; OIDC federation; `allowed_bots` | github-actions |
| Routines | claude.ai/code/routines or `/schedule`; triggers: cron, one-off, API endpoint (`POST /fire`), GitHub events; managed infra, no permission prompts | routines |
| Artifacts | published HTML/MD pages at claude.ai/code/artifact; private/org/public; MCP connectors for live data; comments; capabilities (db, users, assets) | artifacts |
| Chrome | browser extension: click, form-fill, screenshot, console/network, GIF | chrome |
| Claude Tag (Slack) | Team/Enterprise only (not Pro/Max, not third-party deployments); distinct from the older Claude Code in Slack, which stays the setup path on Pro/Max where Claude Tag isn't available; **Public Beta** per the authoritative `claude.com/docs/claude-tag/*` docs (`code.claude.com/docs/en/claude-tag.md` is a short pointer page to them); @Claude in channels; an admin-configured **Access bundle** scopes which connected services/tools Claude can reach per channel/workspace/org; each task runs in an **ephemeral sandbox** created per conversation and discarded when it goes idle, not on a user's machine; channel/thread work draws from an org-funded usage balance with an admin spend limit, but a **direct message is the billing exception** — it runs on the sender's own claude.ai account/seat limits, not the org balance; **routines** (`@Claude !routines`) are standing scheduled work sourced by the channel: cron-style jobs, channel watching, and single-PR subscriptions | claude-tag |
| Settings precedence | managed (policy) is highest and cannot be overridden; then command-line `--settings`, local (`.claude/settings.local.json`), project (`.claude/settings.json`), user (`~/.claude/settings.json`) — corrected by P14 against the live `settings.md`; global state `~/.claude.json` | settings-reference (re-checked 2026-09-07) |
| Sessions | `/resume <id>`, `/branch`, `/fork`, `--teleport`; `/rewind` checkpoints | sessions, checkpointing |
| Context & cost | `/context [all]`, `/compact [instructions]`, `/autocompact` (the documented command; the setting name behind it must be quoted from the live `settings.md`, not assumed — P14); `/cost`, `/usage` incl. per-model + cache hit ratio; `total_cost_usd` in JSON output | context-window, costs |
| Desktop / web | Desktop (macOS/Windows/Linux): scheduled tasks, iOS simulator pane, Remote Control handoff, Cowork tab; Code-tab sessions get their own git worktree, default path `<project-root>/.claude/worktrees/` (configurable); Code-tab permission modes are Manual/Accept edits/Plan/Auto plus Bypass permissions (once enabled) — `dontAsk` is CLI-only; Desktop's Code tab connects to Anthropic's API by default and needs the separate "Claude Desktop on 3P" setup to run on Amazon Bedrock, Google Cloud's Agent Platform or Microsoft Foundry (not the plain CLI/IDE-extension-only claim); **Dispatch** — messaging a task from the Claude mobile app's Cowork tab that Desktop can spawn as a Code-tab session (Pro/Max only, not Team/Enterprise), badged "Dispatch" in the sidebar, its app-use approvals expiring after 30 min instead of lasting the session; `claude remote-control` is a real CLI subcommand (starts a Remote Control server) documented on `cli-reference.md`, alongside the `--remote-control`/`--rc` flag and `--tmux` (creates a tmux/iTerm2-pane session for a worktree, requires `--worktree`); web: cloud sessions (Accept edits/Plan/Auto only, no Manual/Bypass), routines; `--remote` is a deprecated alias for `--cloud` | desktop-quickstart, claude-code-on-the-web, cli-reference |
| IDE | VS Code and JetBrains extensions: inline chat, diff view, selection context; both expose a built-in `ide` MCP server, but the model-visible tool set differs — JetBrains exposes only the read-only `mcp__ide__getDiagnostics` (editor errors/warnings, optionally scoped to one file), while VS Code additionally exposes `mcp__ide__executeCode`, which runs Python in the active Jupyter kernel and always requires a native Quick Pick confirmation (Execute/Cancel) separate from any `PreToolUse` hook or allowlist entry, and refuses outright with no active notebook, no Jupyter extension, or a non-Python kernel (verified `vs-code.md`, `jetbrains.md` 2026-09-08) | vs-code, jetbrains |
| Keybindings / statusline | `~/.claude/keybindings.json`; Shift+Tab mode toggle; `/statusline` | keybindings, statusline |
| Orchestration | Workflow tool (pipeline/parallel), `ultracode`, agent teams, cross-session messaging (SendMessage/ListAgents), worktrees (`isolation: worktree`) | workflows, agent-teams, worktrees |

## Learning resources (resolved 2026-09-06, P03)

| Item | Outcome |
|---|---|
| Anthropic Academy course | **Verified** — *Claude Code in Action*, https://academy.claude.com/courses/claude-code-in-action (free, 9 lessons + quiz, ~1 h). The older `anthropic.skilljar.com/claude-code-in-action` address points at the same course. |
| Anthropic YouTube playlist | **Channel verified** (https://www.youtube.com/@anthropic-ai); **playlist titles/IDs not verifiable programmatically** on 2026-09-06 (client-rendered). Do not cite a playlist ID; cite individual videos after opening them and recording title · channel · duration. |
| Community list | **Verified** — https://github.com/hesreallyhim/awesome-claude-code (active, 53k+ stars). Alternatives seen but not vetted: `subinium/awesome-claude-code`, `jqueryscript/awesome-claude-code`. |
| Community blogs | **Not curated — do not cite by default.** A blog may be cited only when the same claim is confirmed by an official doc; record it in the sources index (meta/03). |

## Fetch log (P03, 2026-09-06)

Re-fetched this session and reconciled above: `hooks.md`, `skills.md`, `sub-agents.md`, `model-config.md`, `tools-reference.md`, `academy.claude.com/courses/claude-code-in-action`, `github.com/hesreallyhim/awesome-claude-code`. Earlier the same day a research agent fetched the docs map and the remaining pages listed in the inventory table.
