# Changelog

<!-- Fixture for the "parser broke" assertion: the upstream file has been restructured so
     that no `## <semver>` heading exists any more. The parser must fail loudly rather
     than report "no drift". -->

### Release 2.1.265

* Added support for pointing `--plugin-dir` at a folder of plugins
